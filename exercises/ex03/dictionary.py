"""ex03 assignment for Dictionary Functions"""

__author__: str = "730705563"


def invert(inverted: dict[str,str]) -> dict[str,str]:
    """invert function that will invert the keys and the values"""
    invert_dict: dict[str,str] = {}


def count(count_numbers: list[str]) -> dict[str,int]:
    """Loops through function, adding 1 if in dictionary or assigning with 1 if not in dictionary"""
    count_dict: dict[str,int] = {}
    first_word: str
    for c in count_dict:
        if first_word in count_dict:
            count_dict[first_word] += 1
        else:
            count_dict[first_word] = 1
    return count_dict


def favorite_color(names_colors: dict[str,str]) -> str:
    """Dictionary for favorite colors and names, and returns the most frequently written color"""
    names_colors_dict: dict[str,str]
    fav_color: str
    for colors in names_colors_dict:
        if fav_color in names_colors_dict:
            print(fav_color)
        else
            print(fav_color[0])
    return fav_color


def bin_len(bins: list[str]) -> dict[int,str]:
    """Uses a list of strings, and turns it into a dictionary, with the key being the length of the words in the list and the value being the words that match the key"""
    sorted_bins: dict[int,str] = {}
    int = len(sorted_bins)
    while len(sorted_bins) = str:
        print dict[int,str]
